/*一个64位的整数可以被看成64位数组，索引为0对应最不重要的位，索引为63对应最重要的位。实现
代码，输入64位的整数，交换索引i和j的比特位。*/
/*0000...01
  0000...10


	0000...01
	0000...11
	0000...10*/
function swap_bits(x,i,j){
	if(((x>>i)&1)!=((x>>j)&1)) {
		//如果不同，则不同两位则必为1和0，可以将不同的两位找到将它们都变为1
		//要想交换位改变，将原来数字和它相xor就行。
		x^=(1<<i) | (1<<j);
	}
	return x;
}